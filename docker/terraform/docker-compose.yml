version: "3.7"
services:
  terraform:
    build:
      context: .
    volumes:
      - ../../:/usr/local/src
      - $HOME/.aws:/root/.aws:ro
    environment:
      - ENV
      - WRI_API_AWS_CORE_INFRASTUCTURE_DEBUG=1
      - AWS_ACCESS_KEY_ID
      - AWS_SECRET_ACCESS_KEY
      - AWS_DEFAULT_REGION
      - CLOUDFLARE_EMAIL
      - CLOUDFLARE_API_KEY
    working_dir: /usr/local/src
    entrypoint: infra
