apiVersion: v1
kind: Service
metadata:
 name: control-tower
spec:
  type: NodePort
  ports:
  - name: http
    protocol: TCP
    port: 9000
    nodePort: 31000
    targetPort: 9000
  selector:
    app: control-tower
