apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: gateway-ingress
  annotations:
    kubernetes.io/ingress.global-static-ip-name: ct-staging-static-ip
    kubernetes.io/tls-acme: "true"
    kubernetes.io/ingress.class: "gce"
spec:
  tls:
  - hosts:
    - staging.pingkapong.com
    - ui-staging.pingkapong.com
    secretName: ct-staging-tls
  rules:
  - host: staging.pingkapong.com
    http:
      paths:
      - path: /*
        backend:
          serviceName: nginx
          servicePort: 80
  - host: ui-staging.pingkapong.com
    http:
      paths:
      - path: /*
        backend:
          serviceName: ct-ui
          servicePort: 80
